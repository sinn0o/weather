# 🌍 세계 랜드마크 날씨 사이트 — PRD

**문서 버전**: v1.0
**작성일**: 2026-08-18
**용도**: 포트폴리오 / 사이드 프로젝트 기획 문서
**작성자**: (본인 이름 기입)

---

## 1. 개요 (Overview)

전 세계 주요 랜드마크를 일러스트 씬으로 탐색하며, 해당 도시의 **실시간 날씨와 현지 시간**을 확인할 수 있는 인터랙티브 웹 사이트. 선택한 랜드마크의 날씨 상태(맑음/비/눈 등)와 시간대(낮/밤)에 따라 메인 화면의 배경 일러스트가 자연스럽게 전환되는 것이 핵심 경험이다.

### 1.1 배경 및 목적
- 단순 날씨 정보 조회를 넘어, **비주얼 스토리텔링**으로 날씨 데이터를 소비하는 경험 제공
- 세계 각지의 랜드마크를 통해 지리적 흥미와 탐색 재미 부여
- 포트폴리오용 프로젝트로서 프론트엔드 인터랙션/애니메이션 역량 및 외부 API 연동 역량 시연

### 1.2 핵심 가치 제안 (Value Proposition)
> "날씨를 숫자가 아니라 장면(scene)으로 본다."

---

## 2. 목표 (Goals & Success Metrics)

| 목표 | 지표 (포트폴리오 기준) |
|---|---|
| 매력적인 첫인상 | 랜딩 후 3초 이내 첫 씬 로딩 완료 |
| 직관적인 탐색 | 클릭 3회 이내에 원하는 도시 날씨 확인 가능 |
| 완성도 있는 인터랙션 | 로딩/에러/빈 상태 등 엣지케이스 100% 커버 |
| 기술 시연 | Open-Meteo API 연동, 반응형 레이아웃, 상태 기반 배경 전환 구현 |

### Non-goals (이번 스코프에서 제외)
- 회원가입/로그인, 즐겨찾기 저장(서버 기반)
- 시간별/주간 상세 예보(우선 "현재 날씨"에 집중)
- 다국어(i18n) — 1차는 한국어(또는 영어) 단일 언어로 시작

---

## 3. 타겟 사용자

- 날씨 앱보다 **비주얼/감성적인 경험**을 선호하는 일반 사용자
- 여행 콘텐츠, 감성 대시보드에 관심 있는 사용자
- (부차적) 포트폴리오 열람자 — 채용 담당자, 동료 개발자

---

## 4. 정보 구조 (IA) & 화면 흐름

```
[랜딩/메인 화면]
 ├─ 상단: 검색창(자동완성)
 ├─ 좌/하단: 필터 바 (날씨 상태 / 대륙·지역 / 낮·밤)
 ├─ 중앙: 랜드마크 리스트 카드 (그리드)
 └─ 카드 클릭
      └─ [메인 씬 화면 전환]
           ├─ 배경: 날씨+시간대 조합 일러스트
           ├─ 랜드마크 실루엣/일러스트 오버레이
           ├─ 정보 패널: 도시명/국가, 현재 시간(현지), 날씨 상태 아이콘,
           │              기온, 체감온도, 습도, 풍속 등
           └─ 뒤로가기 / 다른 랜드마크 리스트 재노출(사이드 패널 or 하단 시트)
```

- **로딩 화면**: 날씨 데이터 요청 중 스켈레톤 UI 또는 일러스트 로딩 애니메이션
- **에러 화면**: API 실패/네트워크 오류 시 재시도 버튼 포함한 전용 상태 화면

---

## 5. 핵심 기능 명세 (Functional Requirements)

### 5.1 랜드마크 리스트 카드 [필수]
- 대륙별 대표 도시 **20~30개**를 큐레이션하여 MVP 데이터셋으로 구성
  - 예: 파리(에펠탑), 도쿄(도쿄타워/센소지), 뉴욕(자유의 여신상), 시드니(오페라하우스), 카이로(피라미드), 리우데자네이루(예수상) 등
- 카드 구성 요소: 랜드마크 일러스트 썸네일, 도시명, 국가, 현재 날씨 아이콘, 현재 기온(요약)
- 카드 클릭 시 메인 씬으로 전환 (상세 화면 이동 또는 모달/오버레이)
- 카드 리스트는 그리드 레이아웃, 필터/검색 결과에 따라 동적 필터링

### 5.2 메인 씬 (일러스트 배경 전환) [핵심 차별 기능]
- 배경 일러스트는 **날씨 상태 × 낮/밤** 조합의 매트릭스로 구성
  - 날씨 상태 카테고리(예): 맑음, 구름많음, 흐림, 비, 뇌우, 눈, 안개
  - 시간대 카테고리: 낮, 노을(일출/일몰 인접 시간대), 밤
  - → 최소 조합 수 설계 필요 (예: 7가지 날씨 × 3가지 시간대 = 21종, 혹은 축소하여 관리 가능한 세트로 시작)
- 일러스트는 SVG 또는 레이어드 PNG로 구성해 CSS 트랜지션/크로스페이드로 부드럽게 전환
- 랜드마크 실루엣은 도시별 고유 에셋, 배경(하늘/조명)은 공통 씬 세트를 재사용하는 구조 권장 (에셋 제작 비용 절감)
- 현지 시간은 타임존 기준 실시간 표시 (초 단위 갱신 불필요, 분 단위로 충분)

### 5.3 검색창 [필수]
- 큐레이션된 랜드마크(도시) **내에서만** 검색 가능한 자동완성 방식
- 도시명(한글/영문) 입력 시 실시간 필터링 리스트 노출
- 검색 결과 없음 상태 별도 처리 (예: "등록된 랜드마크가 없어요" + 추천 도시 노출)

### 5.4 필터 [필수]
필터는 아래 3가지 기준을 조합 가능하도록 설계:
1. **현재 날씨 상태별**: 맑음 / 흐림 / 비 / 눈 / 기타
2. **대륙·지역별**: 아시아 / 유럽 / 북미 / 남미 / 아프리카 / 오세아니아
3. **낮/밤(현지 시간 기준)**: 낮 / 밤

- 필터는 다중 선택 가능(예: "유럽" + "밤"인 랜드마크만 보기)
- 필터 적용 시 리스트 카드 실시간 재정렬/필터링
- 필터 초기화(Reset) 버튼 제공

### 5.5 로딩 화면 [필수]
- 최초 진입 시: 전체 랜드마크의 날씨 데이터 로딩 → 스켈레톤 카드 UI
- 랜드마크 클릭 후 상세 씬 진입 시: 해당 도시 상세 날씨 로딩 → 씬 전환 로딩 애니메이션(일러스트 톤앤매너 유지)
- 로딩이 일정 시간(예: 5초) 초과 시 "시간이 오래 걸리고 있어요" 안내 문구 노출 고려

### 5.6 에러 화면 [필수]
- API 요청 실패(네트워크 오류, 429, 5xx 등) 시 전용 에러 상태 UI
- 재시도 버튼 제공
- 부분 실패 허용: 리스트 전체 로딩 중 일부 도시만 실패 시, 해당 카드만 "정보 없음" 처리하고 나머지는 정상 노출 (전체 화면 에러로 막지 않음)
- 완전 실패(전체 API 다운) 시에만 풀스크린 에러 화면

---

## 6. 데이터 & API 설계

### 6.1 사용 API — Open-Meteo
- **날씨 데이터**: Open-Meteo Forecast API (`current_weather` 또는 `current` 파라미터로 기온, 날씨 코드, 풍속 등 조회)
- **좌표**: 큐레이션된 랜드마크는 위경도를 정적 데이터(JSON)로 미리 저장 (Geocoding API 호출 최소화)
- **현지 시간**: Open-Meteo 응답의 `timezone` 정보 활용 또는 좌표 기반 타임존 라이브러리(day.js + timezone plugin 등) 사용
- **날씨 코드 매핑**: Open-Meteo의 WMO Weather Code를 자체 정의한 "날씨 카테고리"(맑음/흐림/비/눈 등)로 매핑하는 테이블 필요

### 6.2 데이터 모델 (예시)
```json
{
  "id": "paris-eiffel",
  "cityName": "파리",
  "country": "프랑스",
  "continent": "유럽",
  "landmarkName": "에펠탑",
  "lat": 48.8584,
  "lon": 2.2945,
  "timezone": "Europe/Paris",
  "illustrationSet": "paris-eiffel",
  "thumbnail": "/assets/thumbs/paris.png"
}
```

### 6.3 갱신 주기
- 카드 리스트: 페이지 진입 시 1회 fetch, 이후 5~10분 간격 폴링(선택) 또는 수동 새로고침
- 상세 씬: 진입 시 1회 fetch (필요시 폴링)

---

## 7. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 |
|---|---|
| 반응형 | 데스크톱 우선 설계, 태블릿/모바일 대응(브레이크포인트 3단계 권장) |
| 성능 | 일러스트 에셋 최적화(SVG 우선, 이미지 lazy load) |
| 접근성 | 색상 대비, 날씨 아이콘에 대체 텍스트 제공 |
| API 부하 관리 | 다수 카드 동시 조회 시 요청 배칭 또는 캐싱(예: 5분 캐시) |
| 브라우저 지원 | 최신 Chrome/Edge/Safari 기준 |

---

## 8. 기술 스택 제안

- **프론트엔드**: React (또는 Next.js) + TypeScript
- **스타일링**: Tailwind CSS 또는 CSS Modules (씬 전환 애니메이션은 Framer Motion 고려)
- **API 통신**: fetch/axios + React Query(캐싱/폴링 관리에 유리)
- **일러스트 에셋**: Figma로 씬 제작 → SVG export, 또는 Lottie 애니메이션 적용 시 더 풍부한 전환 효과 가능
- **배포**: Vercel/Netlify

---

## 9. 화면별 필수 상태 체크리스트

- [ ] 검색창: 기본 / 입력 중(자동완성) / 결과 없음
- [ ] 리스트 카드: 로딩(스켈레톤) / 정상 / 부분 실패(개별 카드) / 필터 결과 없음
- [ ] 메인 씬: 로딩 / 정상 / 에러(재시도)
- [ ] 전역 에러: 전체 API 다운 시 풀스크린 에러

---

## 10. 향후 확장 아이디어 (Out of MVP scope)

- 사용자 위치 기반 "내 근처 랜드마크" 추천
- 랜드마크별 여행 정보/한줄 소개 카드
- 시간대별(아침/점심/저녁) 씬 자동 순환 데모 모드
- 즐겨찾기(로컬스토리지 기반)
- 다국어 지원

---

## 11. 마일스톤 (제안)

| 단계 | 내용 |
|---|---|
| 1주차 | 랜드마크 20~30개 데이터셋 확정 + Open-Meteo 연동 테스트 |
| 2주차 | 리스트/카드/검색/필터 UI 구현 |
| 3주차 | 메인 씬 + 일러스트 에셋(우선 5~7세트로 축소 시작) + 전환 애니메이션 |
| 4주차 | 로딩/에러 상태 다듬기 + 반응형 대응 + 배포 |

---

## 미결정 사항 (Open Questions)
- [ ] 정확한 20~30개 랜드마크 리스트 확정 (대륙별 안배 기준 필요)
- [ ] 날씨×시간대 일러스트 조합 수 최종 확정 (에셋 제작 리소스에 따라 축소 가능)
- [ ] 부분 실패 시 카드 UI 문구/디자인 톤
